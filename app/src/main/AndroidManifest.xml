<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.academy.learnprogramming">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.SelectEnv" />
        <activity android:name=".ui.login.SignupActivity" />

        <activity android:name=".ui.login.t6env" />
        <activity android:name=".ui.login.t6env2" />
        <activity android:name=".ui.login.activeUsers" />

        <activity android:name=".ui.login.t13env" />
        <activity android:name=".ui.login.t13env2" />
        <activity android:name=".ui.login.EnvActivity" />




    </application>

</manifest>